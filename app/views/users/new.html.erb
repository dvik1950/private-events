<div class="notification">
  As this is an exercise website and is not meant for real use, the password is ommited and you will be able to sign in using only your e-mail address.
</div>

<%= form_for @user, url: {action: "create"} do |f| %>

  <div class="field">
  <div class="control">
  <%= f.label :name, class: "label" %>
  <%= f.text_field :name, class: "input", autofocus: true%>
  </div>
  </div>

  <div class="field">
    <div class="control">
      <%= f.label :email, class: "label" %>
  <%= f.email_field :email, class: "input" %>
    </div>
  </div>

  <%= f.submit "Create account", class: "button is-medium" %>
<% end %>